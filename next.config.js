/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['tinybackend-dev.tinylittle.xyz', 'tinylittlefly.io'],
  },
}

module.exports = nextConfig
